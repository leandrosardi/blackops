# Description:
# - This script is used to start the Sinatra webserver.
# 

#stop_command = 'ps ax | grep "puma" | grep -v postgres | grep -v grep | cut -b1-7 | xargs -t kill -9'
#start_command = 'export RUBYLIB=$HOME/code1/master && cd $HOME/code1/master && source /etc/profile.d/rvm.sh && ruby $HOME/code1/master/launch.rb'

# start webserver
RUN export RUBYLIB=$HOME/code1/master && \
    cd $HOME/code1/master && \
    source /etc/profile.d/rvm.sh && \
    ruby $HOME/code1/master/launch.rb



