# Description:
# Creates a sandbox marker file for the specified SSH user and code folder.
# 

RUN mkdir -p /home/$$ssh_username/code1/$$code_folder
RUN touch /home/$$ssh_username/code1/$$code_folder/.sandbox
